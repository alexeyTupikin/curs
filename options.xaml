<Page x:Class="curs.options"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:curs"
      mc:Ignorable="d" 
      d:DesignHeight="370" d:DesignWidth="650"
      Title="options">

    <Grid>
        <StackPanel Margin="2" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock  Margin="10" Text="Введите имя комптьютера в формате LAPTOP-T46QBGV8\SQLEXPRESS01:"/>
            <TextBox x:Name="computer" Margin="10" Style="{DynamicResource copytewxt}" />
            <TextBlock Margin="10" Text="Введите имя базы данных:"/>
            <TextBox x:Name="database" Margin="10" Style="{DynamicResource copytewxt}" />
            <Button Height="24"  Style="{StaticResource ButtonTheme}" Width="150" Content="Сохранить" Click="saveSettings"/>
            <Button Height="24"  Style="{StaticResource ButtonTheme}" Width="60" Margin="10" Content="Отмена" Click="returnToLogin"/>
        </StackPanel>
    </Grid>
</Page>
